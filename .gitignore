# Android build outputs
android/app/build/
android/build/
android/.gradle/
android/local.properties

# Android release signing (NEVER commit this)
*.jks
*.keystore

# Gradle wrapper (optional to commit — keeping it excluded to save space)
# android/gradle/
# android/gradlew
# android/gradlew.bat

# Node / misc
node_modules/
.DS_Store
Thumbs.db
*.log

# VS Code
.vscode/
